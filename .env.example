# Environment is split between backend and frontend:
#
#   backend/.env      — backend (FastAPI / Alembic)
#   publish/.env      — frontend (SvelteKit / Vite)
#
# Copy each example file to its corresponding .env:
#   cp backend/.env.example backend/.env
#   cp publish/.env.example publish/.env
